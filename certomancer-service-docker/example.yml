external-url-prefix: "http://ca.example.com"

on-demand-settings:
  redis-host: localhost
  key-search-dir: /home/matthias/git/projects/certomancer

keysets:
  testing-ca:
    path-prefix: tests/data/keys-rsa
    keys:
      root:
        path: root.key.pem
        password: secret
      interm:
        path: interm.key.pem
        password: secret
      tsa:
        path: tsa.key.pem
        password: secret
      tsa2:
        path: tsa2.key.pem
        password: secret
      interm-ocsp:
        path: interm-ocsp.key.pem
        password: secret
      signer1:
        path: signer.key.pem
        password: secret
      signer2:
        path: signer2.key.pem
        password: secret
